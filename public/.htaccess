<IfModule mod_rewrite.c>
    RewriteEngine On

    # Redirige tout vers index.php sauf les fichiers ou dossiers existants
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^ index.php [QSA,L]
</IfModule>
